import React from "react";
import {Card, Container} from "react-bootstrap";

function Main() {
  return (
    <Container fluid>
      <Card> Timer </Card>
    </Container>
  )
}

export default Main;
